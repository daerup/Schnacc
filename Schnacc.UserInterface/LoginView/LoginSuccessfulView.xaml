<UserControl x:Class="Schnacc.UserInterface.LoginView.LoginSuccessfulView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Schnacc.UserInterface.LoginView"
             d:DataContext="{d:DesignInstance local:LoginSuccessfulViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200*"/>
            <RowDefinition Height="125*"/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="125*"/>
            <RowDefinition Height="125*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="200*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Label 
            Content="Main Menu"
            Style="{StaticResource PressStartFontTitle}"
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Center"
            Grid.Row="0"
            Grid.Column="1"/>
        <Button 
            Content="Play" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center" 
            Height="89" 
            Width="210"
            Grid.Row="1"
            Grid.Column="1"
            Command="{Binding GoToPlayareaView}"/>

        <Button 
            Content="Highscore" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center" 
            Height="89" 
            Width="210"
            Grid.Row="3"
            Grid.Column="1"
            Command="{Binding GoToHighscoreView}"/>
    </Grid>
</UserControl>
